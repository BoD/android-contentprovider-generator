/**
 * A simple way to convert this project into a Intellij IDEA project, by running `gradle idea`.
 */
apply plugin: 'idea'

idea {
    module {
        jdkName = '1.8'
        downloadJavadoc = true
        downloadSources = true
    }
}