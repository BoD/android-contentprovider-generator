<manifest package="org.jraf.androidcontentprovidergenerator.sample"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat">

        <provider
            android:name=".provider.SampleProvider"
            android:authorities="org.jraf.androidcontentprovidergenerator.sample.provider"
            android:exported="false" />

        <activity
            android:name=".app.SampleActivity"
            android:icon="@drawable/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>